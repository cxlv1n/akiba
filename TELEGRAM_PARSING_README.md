# Интеграция парсинга Telegram канала @akibaautovl

## Описание

Система автоматически парсит посты из Telegram канала @akibaautovl и создает записи автомобилей в базе данных на основе структурированного формата постов.

## Требования

### 1. Telegram API ключи

Для работы с Telegram API необходимо получить API ID и API Hash:

1. Перейдите на https://my.telegram.org/auth
2. Авторизуйтесь и создайте приложение
3. Скопируйте `api_id` и `api_hash`

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
# Telegram API credentials
TELEGRAM_API_ID=your_api_id_here
TELEGRAM_API_HASH=your_api_hash_here
```

Или установите переменные окружения в системе.

## Установка и настройка

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Применение миграций

```bash
python manage.py migrate
```

### 3. Создание директории для медиафайлов

```bash
mkdir -p media/telegram_images
```

## Формат парсинга постов

Система распознает посты со следующей структурой:

```
Год: 2022
▫️Объем: 1,4л(Т)
▫️Оригинальный окрас
▫️Пробег: 32 000км
▫️R-Line Lite 2wd

💰2 100 000₽ под ключ во Владивостоке
+фото
```

### Распознаваемые поля:

- **Год**: `Год: 2022`
- **Объем двигателя**: `Объем: 1,4л(Т)`
- **Пробег**: `Пробег: 32 000км`
- **Цена**: `💰2 100 000₽`
- **Дополнительные характеристики**: Автоматически определяются по ключевым словам

## Использование

### Ручной запуск

```bash
# Парсинг последних 50 постов
python manage.py parse_telegram

# Парсинг последних 20 постов
python manage.py parse_telegram --limit 20

# Тестовый режим (без сохранения в БД)
python manage.py parse_telegram --dry-run

# Парсинг другого канала
python manage.py parse_telegram --channel @other_channel
```

### Автоматический запуск

#### Вариант 1: Cron job (Linux/Mac)

Добавьте в crontab (`crontab -e`):

```bash
# Запуск каждый час в рабочее время (9:00-18:00)
0 9-18 * * 1-5 /path/to/project/scripts/parse_telegram.sh

# Или каждый день в 10:00
0 10 * * * /path/to/project/scripts/parse_telegram.sh
```

#### Вариант 2: Использование скрипта напрямую

```bash
# В cron или планировщике задач
*/30 * * * * /path/to/project/scripts/parse_telegram.sh
```

## Архитектура

### Модели

- **Car**: Основная модель автомобиля (расширена полями для Telegram)
  - `telegram_post_id`: ID поста в Telegram
  - `telegram_channel`: Имя канала
  - `telegram_post_date`: Дата поста
  - `is_from_telegram`: Флаг источника

- **TelegramPost**: История парсинга постов
  - `post_id`: ID поста
  - `channel_username`: Имя канала
  - `parsed_successfully`: Статус парсинга
  - `parsing_error`: Ошибки парсинга

### Сервисы

- **TelegramParser**: Основной сервис для работы с Telegram
  - Подключение к API
  - Получение постов
  - Парсинг контента
  - Скачивание изображений

## Мониторинг

### Логи

Система ведет подробные логи в Django logging. Проверьте `logs/django.log` для отладки.

### Админка

В Django админке доступны:
- Просмотр созданных автомобилей
- История парсинга постов
- Статистика по каналам

## Диагностика

### Распространенные проблемы

1. **"Telegram API credentials not configured"**
   - Проверьте переменные окружения
   - Убедитесь, что файл `.env` существует

2. **"No new cars were created"**
   - Проверьте формат постов в канале
   - Возможно, посты уже обработаны ранее

3. **Ошибки сети**
   - Проверьте подключение к интернету
   - Возможно, ограничения Telegram API

### Тестирование

```bash
# Тестовый запуск без сохранения
python manage.py parse_telegram --dry-run --limit 5
```

## Безопасность

- API ключи хранятся в переменных окружения
- Система не отправляет сообщения в канал
- Только чтение публичных каналов
- Изображения сохраняются локально

## Производительность

- Ограничение на количество постов (по умолчанию 50)
- Кэширование результатов парсинга
- Асинхронная обработка через Telethon
- Проверка на дубликаты по `telegram_post_id`